Trap = class
{
	constructor(x,y,width,height)
	{
		this.x = x;
		this.y = y;
		this.rotation = 0
		this.spinner;
	}
	collision(player)
	{
		throw new Error('You have to implement the abstract method');
	}
	update()
	{
		throw new Error('You have to implement the abstract method');
	}
}